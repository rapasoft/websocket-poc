# WebSockets PoC
Backend for WebSocket Proof of Concept

Build it with `mvn clean package` and run it using `mvn spring-boot:run`

It should be then communicating with server running on `http://localhost:3000`, see [Frontend part](https://github.com/rapasoft/websockets-frontend) on how to communicate with backend.
